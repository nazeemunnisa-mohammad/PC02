import 'package:flutter/material.dart';

class ScrollViewTestCase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Test Case 2")),
      body: SingleChildScrollView(
        padding: EdgeInsets.all(16),
        child: Column(
          children: [
            TextField(),
            SizedBox(height: 500), // To demonstrate scrolling
            Text("Scroll view prevents bottom overflow"),
          ],
        ),
      ),
    );
  }
}
